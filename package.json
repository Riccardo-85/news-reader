{
  "name": "news-reader",
  "private": true,
  "version": "1.0.0",
  "description": "React + Vite news reader with an Express proxy for TheNewsApi",
  "scripts": {
    "server:install": "npm --prefix server install",
    "server:dev": "npm --prefix server run dev",
    "web:dev": "npm --prefix web run dev",
    "dev": "concurrently -k -n server,web -c blue,green \"npm run server:dev\" \"npm run web:dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
